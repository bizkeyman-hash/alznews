# AlzNews Environment Variables
# Copy this file to .env.local and fill in values as needed.
# API keys are optional — the app works with RSS feeds alone.

# NewsAPI.org (optional, enables additional English news)
NEWSAPI_KEY=

# Naver Open API (optional, enables Korean news search)
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# Cache TTL in seconds (default: 900 = 15 minutes)
CACHE_TTL_SECONDS=900

# Secret for POST /api/revalidate cache clear endpoint (optional)
REVALIDATE_SECRET=
